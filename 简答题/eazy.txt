1. 类方法的重写和重载分别是什么意思？
重写: override, 子类覆盖父类方法实现
重载: overload, 让同名方法有不同参数数量的组合

2. 线程与进程的区别
线程是进程的一个实体,是CPU调度和分派的基本单位,它是比进程更小的能独立运行的基本单位.
线程自己基本上不拥有系统资源,只拥有一点在运行中必不可少的资源(如程序计数器,一组寄存器和栈),但是它可与同属一个进程的其他的线程共享进程所拥有的全部资源.
 2.1扩展:  资源指什么, 线程共享的资源指什么, 线程有没有不共享的资源, java中想修改这个资源的大小怎么指定, 其默认多大( 512K/1024K都算对, 过去老版本和32位jvm是512k, 64位jdk8是1024K )

3. BST(二分搜索树)的哪个遍历方式得到的数据是排好序的?
中序
3.1 (扩展问题: 中)我们常用的BST实现类是哪些? 
avl树和红黑树


4. char foo = '中', 是否正确? (源文件以UTF8编码存储，并且以javac – encoding utf-8命令编译)
正确: 
扩展回答(扩展偏难了)
这里的’中‘应该是不管用什么编码方式存储，赋值给char变量后都会先转换成对应的unicode的序号\u4e2d,（知道了项目原编码方式，以及某个字符，然后按对应规则转--见下文a）之后再用UTF-16BE来作为\u4e2d这个字符编码的存储方案。
因此，只要是utf-16be能表示的unicode编码都是可以成功的，与java文件本身的字符编码无关系。
比如，’中‘在gbk中的编码是d6d0，那当赋值给char类型时，java知道这是gbk字符集下表示的’中‘，那就能通过对比gbk和unicode，找到d6d0对应的是\u4e2d.同理，utf-8的’中‘虽然是占了三个字节，但java只会直接把它转成对应的unicode编码，再转成utf16be表示，与它是几个字节无关。

5. 简述jdk的常见集合类及其用途. 
List: ArrayList, LinkedList
Set: HashSet, TreeSet
Map: HashMap, TreeMap

5.1 (扩展问题: 中) ArrayList/HashMap的实现原理, 如何扩容, 如何散列, 散列冲突怎么处理的


6. 待查表什么前提下才可以用二分查找?
有序

6.1 扩展: 请实现
public int indexOf(int[] arr, int x) {// 寻找x所在arr的索引, arr已排序, 假定x一定在arr中存在

}

7. final关键字可以修饰什么, 各什么含义?
修饰类: 类不能被继承
修饰方法: 方法不能被重写
修饰变量: 只能初始化一次, 其后不能更改, 即成为常量




